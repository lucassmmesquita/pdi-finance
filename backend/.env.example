# Ambiente
ENVIRONMENT=development

# Banco de Dados PostgreSQL
DATABASE_URL=postgresql://pdi_user:pdi_password@localhost:5432/pdi_finance
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pdi_finance
DB_USER=pdi_user
DB_PASSWORD=pdi_password

# JWT Authentication
SECRET_KEY=sua-chave-secreta-super-segura-aqui-mude-em-producao
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# API
API_V1_PREFIX=/api
PROJECT_NAME=PDI Finance API
VERSION=1.0.0

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Upload
MAX_UPLOAD_SIZE_MB=10
UPLOAD_DIR=./uploads

# Email (futuro)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
